# 云湃电商 AI 客服产品技术路线

> 状态说明（2026-07-21）：本文件保留为 Agent 内部能力的历史技术参考，不再代表当前开发顺序。当前优先路线已改为先完成平台资质、后台数据接入、客服消息收发和人工接管，具体以 `云湃电商后台接入与客服接管执行调研报告_20260721.md` 及 `.project-to-act/PROJECT_FEATURES.md` 的 F-201 至 F-210 为准。

**版本：** 1.0  
**日期：** 2026-07-21  
**适用阶段：** 当前客服 Agent 0.5.0 至首批真实店铺 PoC  
**文档定位：** 将“以客服为入口、以 SOP 为边界、以 Agent 执行业务、以数据闭环持续进化”的产品思路落实为可开发、可测试、可交付的技术路线。  
**关联文档：** `云湃电商全域AI实现技术路径方案_V1_20260721.md`、`docs/architecture.md`、`AI电商客服多端接入调研报告_20260718.md`

---

## 一、路线结论

云湃不把产品定义为“更会聊天的客服机器人”，而是定义为一套受控的电商客服业务执行系统：

```text
合法渠道接入
  -> 统一会话与可信业务上下文
  -> 商品/店铺/平台/SOP 知识检索
  -> Agent 理解目标并选择工具
  -> 策略门判断自动回复、自动执行或转人工
  -> 确定性工作流执行并验证结果
  -> 质检、VOC、反馈和评测驱动持续优化
```

产品按以下顺序演进：

1. **回复工具：** 聚合接待、知识问答、上下文对话、人工协同；
2. **成交助手：** 商品推荐、SKU 对比、图片识别、跟单、催拍催付；
3. **业务执行：** 低风险售后、工单、物流查询、邀评等可验证动作；
4. **经营系统：** VOC、质检、转化数据和店铺经营诊断；
5. **全域经营 AI：** 与现有经营 AI 技术方案汇合，扩展库存、内容、营销和供应链能力。

V1 的工程重点不是增加 Agent 数量，而是打通一条真实、合法、可审计、可人工接管的闭环。

---

## 二、目标与非目标

### 2.1 V1 产品目标

- 接入至少一个合法客服通道或经过授权的服务商适配器；
- 支持商品、店铺规则、平台规则、SOP 和已批准话术的分层知识；
- 支持多轮上下文、图片内容识别和可信订单上下文；
- 支持仅提示、人机协同、智能转接、夜间值守四种接待模式；
- 支持至少一个低风险、可幂等、可读回验证的真实业务动作；
- 建立回复置信度、业务风险、权限和人工接管的联合放行机制；
- 建立反馈、质检、评测、批准、发布和回滚的知识进化闭环；
- 提供会话、人工任务、知识、SOP、质检和运行状态的管理界面或稳定 API。

### 2.2 V1 非目标

- 不绕过淘宝、京东、抖店等平台授权，不做 Cookie 抓取或客户端注入；
- 不让模型直接掌握平台密钥、订单数据权限或动作放行权；
- 不把退款、赔付、改价、付款作为首个自动写动作；
- 不让多个自由 Agent 互相讨论后直接操作业务系统；
- 不允许自进化自动修改代码、系统 Prompt、权限、阈值或工具定义；
- 不为了“全行业通用”提前建设复杂微服务、Kafka、Kubernetes 或独立向量集群；
- 不把竞品材料中的节省比例、响应时长和独立接待率当作云湃已实现指标。

---

## 三、设计原则

1. **模型负责理解，代码负责放行。** LLM 可以识别目标、选择工具和组织回复，但权限、风险、幂等、状态迁移和成功判定由确定性代码控制。
2. **SOP 优先于 FAQ 堆积。** 知识回答事实，SOP 描述业务目标、条件、步骤、动作和边界。
3. **先证据后结论。** 回复和动作必须引用当前会话、批准知识或已验证工具结果；无证据时澄清或转人工。
4. **先辅助后自动。** 新场景先以仅提示或人机协同上线，通过影子评测后再扩大自动回复和自动执行范围。
5. **风险与置信度分离。** 高置信度不能越过高风险审批，低风险也不能在证据不足时自动执行。
6. **全链路可回放。** 输入、检索证据、模型决策、工具调用、策略结果、人工接管和最终输出均可审计。
7. **单机优先、模块清晰。** 在一体机交付约束下保持模块化单体，只有真实容量和运维需求出现后再拆服务。

---

## 四、目标架构

```text
┌──────────────────────────────────────────────────────────────┐
│ 客服工作台 / 管理后台 / 运营看板                             │
└──────────────────────────┬───────────────────────────────────┘
                           │ HTTPS / SSE / WebSocket
┌──────────────────────────▼───────────────────────────────────┐
│ FastAPI 模块化单体                                          │
│ Channel │ Session │ Knowledge │ SOP │ Agent │ Handoff │ QA │
└──────────────┬──────────────┬──────────────┬────────────────┘
               │              │              │
      ┌────────▼───────┐ ┌────▼────────┐ ┌───▼────────────────┐
      │ Agent Runtime  │ │ Policy Gate │ │ Workflow Runtime   │
      │ LangGraph/RAG  │ │ 权限/风险    │ │ 幂等/重试/读回验证 │
      └────────┬───────┘ └────┬────────┘ └───┬────────────────┘
               │              │              │
┌──────────────▼──────────────▼──────────────▼────────────────┐
│ SQLite（单机 V1）：会话、知识、SOP、任务、审计、评测、指标   │
└──────────────┬───────────────────────────────────────────────┘
               │
      ┌────────▼──────────────────────────────────────────────┐
      │ 合法渠道适配器 / ERP / 订单 / 物流 / 商品 / OCR 服务  │
      └───────────────────────────────────────────────────────┘
```

### 4.1 Agent Runtime 与 Workflow Runtime 分离

**Agent Runtime** 处理：

- 识别顾客目标、情绪、商品/SKU 和缺失参数；
- 选择知识检索、澄清、查询、动作、结束或转人工；
- 在有界 ReAct 中根据 Observation 调整下一步；
- 生成自然语言回复和结构化动作建议。

**Workflow Runtime** 处理：

- 渠道验签、去重、限流和回写；
- SOP 状态迁移、工具执行、重试和超时；
- 写动作幂等、审批、后置验证和补偿；
- 人工任务生命周期和审计记录。

模型不得直接修改工作流状态，也不得仅凭语言判断外部动作成功。

---

## 五、核心技术路线

### 5.1 合法多端接入

定义统一 `ChannelAdapter`：

```python
class ChannelAdapter(Protocol):
    platform: str
    capability_version: str

    async def verify_webhook(self, request) -> VerifiedWebhook: ...
    async def normalize_message(self, payload) -> InboundMessage: ...
    async def send_message(self, message: OutboundMessage) -> SendResult: ...
    async def fetch_context(self, ref: ContextRef) -> TrustedContext: ...
```

适配器必须承担：

- 合法授权、凭据托管和轮换；
- 平台验签、时间戳和重放保护；
- 消息 ID 去重、会话映射和顺序控制；
- 平台限流、错误分类、重试和熔断；
- 图片、商品卡片、订单卡片和富文本归一；
- 发送结果读回或平台回执保存。

首个适配器选择标准：已取得真实权限、文档稳定、支持消息读取和回复、具备沙箱或测试账号。若权限未确认，V1 使用标准适配器协议加文件/模拟回放，不宣称完成真实平台接入。

### 5.2 统一会话与可信上下文

统一消息信封至少包含：

```json
{
  "tenant_id": "tenant-a",
  "store_id": "store-a",
  "platform": "authorized-adapter",
  "conversation_id": "platform-conversation-id",
  "message_id": "platform-message-id",
  "subject_id_hash": "hmac-value",
  "message_type": "text|image|product_card|order_card|system",
  "content": {},
  "occurred_at": "2026-07-21T00:00:00Z",
  "trace_id": "uuid"
}
```

约束：

- 外部主体 ID 先 HMAC，再进入业务库和 checkpoint；
- 订单、物流、会员等级等只能由具有对应能力的认证适配器提供；
- 请求正文自报的 `authorized`、角色、金额阈值一律无效；
- context 在进入 LangGraph checkpoint 前完成白名单清洗和脱敏；
- 同一租户、店铺、渠道和顾客会话形成稳定 thread，跨租户不可合并。

### 5.3 分层知识与 Context Engineering

知识分为五类：

| 层级 | 内容 | 更新方式 | 是否可直接放行动作 |
|---|---|---|---|
| 平台层 | 平台政策、活动规则、禁限售规则 | 人工审核发布 | 否 |
| 行业层 | 行业术语、常见场景、标准话术包 | 产品团队版本化 | 否 |
| 店铺层 | 店铺政策、品牌口径、售后承诺 | 店铺管理员审批 | 否 |
| 商品层 | SPU/SKU、属性、图片、参数、库存引用 | 授权同步或导入 | 否 |
| 进化层 | 人工修正答案、优秀话术、失败案例 | 评测通过并批准 | 否 |

上下文组装顺序：

```text
可信会话状态
  + 当前商品/SKU/订单证据
  + 命中的 SOP 与风险边界
  + 分层知识检索结果
  + 当前可用工具目录
  + 输出格式和会话渠道限制
```

商品事实、店铺政策和 SOP 必须带租户、版本、来源、校验和、状态和审批人。RAG 不承载权限、资金阈值和动作放行规则。

### 5.4 SOP 引擎

SOP 是业务执行的主要配置单元，建议采用版本化 YAML/JSON DSL：

```yaml
id: after_sale_damaged_item
version: 1
trigger:
  intents: [damaged_item]
required_context: [order_id, product_id]
steps:
  - observe: get_order_detail
  - clarify_if_missing: damage_image
  - evaluate: compensation_policy
  - propose: create_after_sale_task
guards:
  max_compensation_cents: 500
  prohibited_claims: [instant_refund_without_verification]
handoff:
  when: [high_value_order, evidence_conflict, customer_escalation]
success:
  postcondition: after_sale_task_created
```

SOP 引擎负责：

- 触发条件、必需上下文和前置检查；
- 可调用工具、允许动作和禁止表达；
- 澄清、人工接管和退出条件；
- 成功后置条件和失败补偿；
- 灰度范围、生效时间和版本回滚。

SOP 发布流程：`draft -> evaluated -> approved -> active -> retired`。任何修改都生成新版本，运行中的会话固定使用启动时版本，避免半途切换行为。

### 5.5 Agent 拓扑

V1 采用一个 Supervisor 加少量领域能力：

```text
inbound_message
  -> identity_and_scope
  -> input_policy
  -> context_builder
  -> supervisor
       -> knowledge_answer
       -> product_advisor
       -> after_sale_assistant
       -> conversation_action
  -> decision_gate
       -> answer / clarify / observe / act / handoff / refuse / finish
  -> output_policy
  -> channel_delivery
  -> persist_and_measure
```

V1 不为每个业务意图建立 LangGraph 节点。商品推荐、物流查询、售后建单等能力通过动态工具目录和 SOP 暴露，只有在 Prompt、工具、状态或评测明显分化时才拆成独立 Agent。

### 5.6 多模态处理

支持的输入类型按风险逐步开放：

1. 商品图和商品卡片识别；
2. 尺码表、参数表和说明书识别；
3. 物流面单、售后破损图和退款凭证辅助识别；
4. 需要身份或金额判断的图片只生成建议，不直接执行。

多模态结果必须结构化保存：识别对象、候选类别、置信度、模型版本、原图引用和人工校验状态。图片识别只是证据来源之一，涉及赔付、退款或责任判断时不得单独作为放行依据。

### 5.7 人机协同与接待模式

| 模式 | AI 权限 | 适用阶段 |
|---|---|---|
| 仅提示 | 生成建议，不自动发送 | 新店铺、新 SOP、低样本场景 |
| 人机协同 | 简单问题自动答，复杂问题建单 | 默认生产模式 |
| 智能转接 | 按规则暂停 AI 并转指定队列 | 投诉、高价值订单、高风险售后 |
| 夜间值守 | 在白名单场景内自动接待 | 影子评测达标后的稳定场景 |

人工工作台必须支持：

- 查看 AI 当前建议、证据、SOP、置信度和风险；
- 一键暂停、接管、修改后发送和恢复 AI；
- 标记错误类型并提供修正答案；
- 将会话转为售后、投诉、技术或主管任务；
- 保留人工修改前后 diff，作为评测和学习数据。

### 5.8 联合风控

最终动作由以下四个维度联合判断：

```text
allow = identity_scope_ok
    and evidence_sufficient
    and confidence_above_threshold
    and risk_policy_allows
    and sop_version_active
```

建议风险分级：

| 等级 | 示例 | 默认策略 |
|---|---|---|
| L0 | 知识查询、商品参数读取 | 可自动回复 |
| L1 | 内部标记、创建人工任务 | 可自动执行并审计 |
| L2 | 发送邀评、催拍、低风险通知 | 灰度后自动执行 |
| L3 | 补发申请、低额补偿建议 | 人工审批后执行 |
| L4 | 退款、改价、赔付、优惠承诺 | V1 只生成方案 |
| L5 | 付款、采购、权限和安全策略 | 禁止模型发起 |

置信度至少综合：知识证据、商品/SKU 匹配、订单身份、上下文一致性、SOP 覆盖、模型自评和历史评测表现。初始阈值必须通过影子数据校准，不在文档中硬编码为统一百分比。

### 5.9 工具和真实动作

现有 `ToolSpec` 应逐步扩展：

- `domain`、`capability_version`、`risk_level`；
- `required_permissions`、`required_context_fields`；
- `approval_policy_id`、`supports_dry_run`；
- `idempotency_fields`、`result_schema`；
- `postcondition_verifier`、`verification_timeout`；
- `compensation_handler` 或 `non_compensatable=true`；
- `data_classification`、`rate_limit_key`。

首批只读工具：

- `search_approved_knowledge`
- `get_product_detail`
- `compare_product_skus`
- `get_order_detail`
- `get_logistics_status`
- `get_store_policy`

首批低风险动作：

- `create_handoff_task`
- `tag_conversation`
- `create_after_sale_task`
- `send_followup_message`（仅白名单模板和频控范围）

第一个真实外部写动作必须满足：官方允许、低资损、可 dry-run 或稳定读回、具备幂等键、失败可见、人工可补偿。

### 5.10 持续学习与评测闭环

```text
人工负反馈 / 质检问题 / 低置信度会话
  -> 候选问题聚类
  -> 人工修正答案与证据
  -> 静态安全检查
  -> 检索碰撞与租户隔离检查
  -> 回放评测与基线回归
  -> 管理员批准
  -> 灰度发布
  -> 在线指标监控
  -> 保留或回滚
```

自进化只更新批准知识、话术和可配置 SOP 草稿。SOP 草稿仍需人工评审，权限、风险策略、工具代码和模型权重不可自动修改。

评测集至少覆盖：

- 商品参数、SKU 对比、活动、订单、物流和售后；
- 多轮省略、指代、改口、重复追问和跨商品切换；
- 图片、表格和商品卡片；
- 提示注入、越权请求、隐私信息和虚假授权；
- 低置信度、工具超时、平台限流、重复消息和人工接管；
- 每个已激活 SOP 的正常、边界、失败和转人工路径。

### 5.11 质检、VOC 与经营数据

质检输出必须区分：

- 事实错误；
- SOP 违规；
- 越权或隐私风险；
- 未识别顾客意图；
- 不必要转人工；
- 应转未转；
- 回复风格和情绪问题；
- 渠道发送、工具或数据故障。

VOC V1 提供问题聚类、异常情绪、热点咨询、询单流失、售后原因和知识缺口分析。所有指标必须代码化定义，不能让模型直接拼 SQL。质检结果回流评测和知识候选，但不自动改变线上规则。

---

## 六、核心数据模型

V1 在现有 SQLite 上增量增加以下实体；进入多进程经营 AI 阶段再按全域技术方案迁往 PostgreSQL。

| 实体 | 关键字段 |
|---|---|
| `channel_connections` | tenant、store、platform、capabilities、secret_ref、status |
| `conversations` | channel、store、subject_hash、mode、sop_version、status |
| `channel_messages` | external_id、direction、type、content_ref、occurred_at、dedupe_key |
| `trusted_context_snapshots` | conversation、source_client、schema_version、payload_hash |
| `knowledge_items` | tenant、layer、source、version、checksum、status、approved_by |
| `sop_definitions` | sop_id、version、dsl、risk_level、status、effective_at |
| `agent_decisions` | mode、tool、arguments_hash、evidence_ids、confidence、risk |
| `tool_executions` | idempotency_key、status、external_request_id、postcondition |
| `handoff_tasks` | queue、owner、reason、priority、status、version |
| `feedback_events` | message_id、error_type、correction、evidence_source |
| `qa_results` | conversation、ruleset_version、issues、score、review_status |
| `eval_cases` / `eval_runs` | dataset_version、expected、actual、result、trace_id |

数据保留遵循现有策略：消息默认 30 天、审计默认 365 天；未决人工任务、审批证据、进化证据和必要交易审计不随普通消息清理。

---

## 七、代码仓库落点

不推倒当前工程，按以下顺序扩展：

| 当前模块 | 保留能力 | 下一步改造 |
|---|---|---|
| `auth.py` | 客户端认证、租户绑定、主体哈希 | 增加 store/channel scope 和适配器能力声明 |
| `graph.py` | 结构化决策、有界 ReAct | 增加 ContextBuilder、SOP 引用和接待模式 |
| `tools.py` | 动态目录、参数校验、幂等、后置验证 | 增加风险、权限、审批和能力版本 |
| `rag.py` | 租户知识和混合检索 | 增加知识分层、SKU/店铺过滤和证据 ID |
| `evolution.py` | 评测、批准、回滚 | 接入住户质检、灰度和知识/SOP 草稿 |
| `handoff.py` | 持久任务和乐观锁 | 增加队列、优先级、SLA 和人工工作台接口 |
| `policy.py` | 输入输出安全门 | 增加业务风险、模式、频控和 SOP 门 |
| `database.py` | SQLite、审计、迁移 | 增加渠道、消息、SOP、执行、质检和评测表 |
| `api.py` | 聊天和管理 API | 拆分 channel、knowledge、sop、handoff、qa 路由 |

建议逐步形成：

```text
src/ecommerce_agent/
  channels/
    base.py
    registry.py
    replay.py
    <first_adapter>/
  context/
    builder.py
    schemas.py
  sops/
    models.py
    engine.py
    repository.py
  agents/
    supervisor.py
    product_advisor.py
    after_sale.py
  workflows/
    execution.py
    verification.py
  qa/
    rules.py
    voc.py
  api/
    chat.py
    channels.py
    knowledge.py
    sops.py
    handoffs.py
    qa.py
```

迁移应按功能切片进行，避免一次性重排现有模块导致 0.5.0 安全基线失效。

---

## 八、API 与事件边界

### 8.1 渠道与会话

```text
POST /v1/channels/{channel}/webhook
POST /v1/channels/{channel}/replay
GET  /v1/conversations
GET  /v1/conversations/{id}
POST /v1/conversations/{id}/pause
POST /v1/conversations/{id}/resume
POST /v1/conversations/{id}/handoff
```

### 8.2 知识与 SOP

```text
POST /v1/knowledge/import
POST /v1/knowledge/{id}/evaluate
POST /v1/knowledge/{id}/approve
POST /v1/knowledge/{id}/rollback
POST /v1/sops
POST /v1/sops/{id}/evaluate
POST /v1/sops/{id}/activate
POST /v1/sops/{id}/retire
```

### 8.3 质检与评测

```text
POST /v1/feedback
POST /v1/qa/runs
GET  /v1/qa/results
POST /v1/evals/runs
GET  /v1/evals/runs/{id}
GET  /v1/voc/overview
```

内部事件统一使用 outbox 语义，至少包括：`message.received`、`decision.created`、`tool.completed`、`handoff.created`、`feedback.received`、`knowledge.activated`、`sop.activated`。单机 V1 可用 SQLite 持久任务轮询，不引入外部消息队列。

---

## 九、分阶段实施计划

按 6-8 人团队估算：后端 3、前端 1、算法/数据 1、测试 1、产品/实施 1；平台接入和安全必须指定 Owner。

| 阶段 | 周期 | 交付内容 | 退出条件 |
|---|---:|---|---|
| M0 基线冻结 | 第 1 周 | ADR、首个客户/行业、平台权限清单、脱敏样本、当前 0.5.0 回归基线 | 权限与数据来源书面确认；现有测试全通过 |
| M1 渠道影子接入 | 第 2-3 周 | `ChannelAdapter`、消息信封、验签/去重、回放工具、只读接入 | 7 天或等价回放无重复、无串会话、无跨租户 |
| M2 知识与上下文 | 第 4-5 周 | 五层知识、SKU/店铺过滤、ContextBuilder、证据 ID | 150 条问答评测达标；严重事实错误为 0 |
| M3 SOP 与人机协同 | 第 6-8 周 | SOP DSL、四种接待模式、人工工作台 API、暂停/恢复/转接 | 每个 SOP 的正常/边界/失败路径均可回放 |
| M4 商品与多模态 | 第 9-10 周 | 商品推荐、SKU 对比、图片/表格识别、结构化证据 | 焦点 SKU 识别和跨商品上下文指标达标 |
| M5 低风险业务执行 | 第 11-12 周 | 首个真实写工具、dry-run、幂等、审批、读回、补偿 | 未授权写入 0；成功状态均有后置验证证据 |
| M6 学习与质检 | 第 13-14 周 | 质检规则、VOC、反馈聚类、知识灰度和回滚 | 新知识提升目标集且基线无严重回归 |
| M7 PoC 放量 | 第 15-16 周 | 脱敏真实流量影子运行、灰度自动回复、运行看板、运维演练 | 连续两周或等价回放通过上线门禁 |

### 9.1 放量顺序

```text
离线回放
  -> 影子模式（只记录不发送）
  -> 仅提示
  -> 人机协同 10%
  -> 人机协同 30%
  -> 白名单场景自动回复
  -> 白名单低风险动作
```

任一阶段出现越权、错误动作、严重事实错误或人工接管失效，立即停止扩大流量并回滚到上一模式。

---

## 十、首批 Epic 与交付定义

1. `EPIC-CHANNEL-SDK`：适配器契约、验签、去重、限流、错误和回放；
2. `EPIC-TRUSTED-CONTEXT`：统一信封、主体哈希、订单上下文能力和脱敏；
3. `EPIC-KNOWLEDGE-LAYERS`：平台/行业/店铺/商品/进化知识和版本；
4. `EPIC-SOP-ENGINE`：DSL、版本、评测、发布、状态迁移和回滚；
5. `EPIC-ASSIST-MODES`：仅提示、人机协同、智能转接、夜间值守；
6. `EPIC-PRODUCT-ADVISOR`：SKU 识别、推荐、对比、证据和话术；
7. `EPIC-MULTIMODAL`：图片、表格、商品卡片和结构化识别结果；
8. `EPIC-ACTION-RUNTIME`：风险、审批、幂等、执行、读回和补偿；
9. `EPIC-HANDOFF-WORKBENCH`：队列、SLA、接管、恢复、diff 和反馈；
10. `EPIC-QA-VOC`：质检、问题聚类、知识缺口和运营指标；
11. `EPIC-EVALS`：离线、回放、影子、故障注入和发布门禁；
12. `EPIC-APPLIANCE-OPS`：本地部署、密钥、备份、恢复、监控和升级。

每个 Epic 的 Definition of Done：

- 数据模型和迁移可重复执行；
- API/工具 schema 和错误语义有文档；
- 单元、集成、权限、隔离和失败路径测试齐全；
- 关键操作生成审计和运行指标；
- 有脱敏样本或模拟器可回放；
- 管理员可发现失败、定位 trace 并执行人工恢复；
- 上线、回滚和数据清理步骤可操作。

---

## 十一、验收指标

### 11.1 安全与正确性硬门槛

- 跨租户、跨店铺、跨会话数据泄露：`0`；
- 未授权写操作：`0`；
- 未经后置验证即声明动作完成：`0`；
- 高风险场景绕过人工审批：`0`；
- 提示注入导致工具越权：`0`；
- 人工暂停后 AI 继续自动发送：`0`；
- 生产日志出现明文平台密钥或完整个人敏感信息：`0`。

### 11.2 质量指标

以下初值用于 PoC Gate，最终值由首个客户样本校准：

| 指标 | 初始目标 |
|---|---:|
| 标准商品/店铺知识问答准确率 | >= 95% |
| 严重事实错误率 | 0% |
| 焦点 SKU 识别准确率 | >= 98% |
| SOP 路径命中率 | >= 95% |
| 应转人工召回率 | >= 99% |
| 不必要转人工率 | <= 15% |
| 重复消息导致重复动作 | 0 |
| 已批准进化知识基线严重回归 | 0 |

### 11.3 性能与运行指标

- 文本咨询 P95 首次响应在目标硬件和模型档位下达到客户 SLA；
- 渠道消息积压、适配器错误、模型错误、检索耗时、工具耗时和人工 SLA 可观测；
- 模型不可用时可降级为批准答案复用、仅提示或人工任务；
- 重启后未完成工作流可恢复，不重复产生外部副作用；
- 备份恢复、授权失效、磁盘不足和模型限流演练通过。

业务指标如客服成本、独立接待率、转化提升和响应时长必须以 PoC 前后同口径数据评估，竞品宣传数据仅作为假设，不作为验收证据。

---

## 十二、部署与运维

V1 保持单机部署：

```text
api process
  + background workflow worker
  + SQLite business database
  + SQLite LangGraph checkpoint
  + encrypted secret store
  + local object directory
```

生产要求：

- 平台密钥与数据库分离保存，支持轮换；
- 数据库和对象目录加密备份，执行恢复演练；
- `/health` 只表示进程存活，`/ready` 检查模型、数据库、磁盘和关键配置；
- 日志按 trace ID 关联，不保存模型隐式思维过程；
- 对消息正文、订单、电话、地址和图片 URL 进行分级脱敏；
- 留存清理默认 dry-run，未决人工任务和审计证据受保护；
- 升级前自动备份并验证迁移，失败可回滚应用和数据版本。

当出现多实例写入、高频渠道同步、复杂经营聚合或多租户集中部署时，再按全域 AI 方案迁移 PostgreSQL、独立 Worker/Scheduler 和对象存储。

---

## 十三、开发启动前 Gate

以下事项未确认前，不进入对应实现：

1. **首个客户和类目：** 明确商品结构、咨询量、售后风险和成功指标；
2. **首个合法渠道：** 获取真实 API/服务商权限、测试账号和能力清单；
3. **首批自动回复场景：** 从高频、低风险、知识稳定的场景中选择；
4. **首个真实写动作：** 必须低资损、可读回、可幂等、可人工补偿；
5. **模型与部署边界：** 确认默认混合模型或严格本地模型及数据出域规则；
6. **人工工作台 Owner：** 明确谁接收任务、SLA、班次和升级路径；
7. **指标口径：** 明确回复准确、独立接待、转人工、转化和成本的统计方式。

未知项保持未定义，不由研发自行假设。

---

## 十四、与全域经营 AI 路线的衔接

客服路线提供经营 AI 所需的入口和数据基础：

```text
客服会话与商品问题
  -> 商品/SKU 知识缺口
  -> 询单、流失、售后和情绪数据
  -> 质检与 VOC 指标
  -> 商品、库存、内容和营销异常
  -> 经营建议、任务、审批和执行
```

两份路线共享身份、连接器、知识、Agent Runtime、Policy、Task/Approval、Action/Execution 和 Audit 模块。区别是：

- 本文优先解决实时会话、人机协同、SOP 和客服动作；
- 全域方案优先解决经营事实、指标语义层、经营诊断和跨域任务；
- 客服 V1 继续使用单机 SQLite；经营数据和多进程需求成熟后迁移 PostgreSQL；
- 两条路线不得分别建设两套权限、任务、审计或工具运行时。

---

## 十五、V1 成功定义

V1 成功不是“AI 能回答更多问题”，而是同时满足：

- 一个真实合法渠道可稳定接入、回放和审计；
- 商品、店铺、平台和 SOP 知识可版本化管理；
- Agent 能在多轮和多模态上下文中给出有证据的回复；
- 人工可以看见、暂停、接管、修改、恢复和反馈；
- 至少一个低风险真实动作具备权限、幂等、审批、读回和补偿闭环；
- 反馈和质检能经过评测、批准、灰度和回滚转化为产品改进；
- 模型、渠道或外部系统故障时，人工流程和审计仍然可用。

达到上述条件后，再扩大自动回复范围、增加行业话术包和领域 Agent，并与全域经营 AI 的任务、审批和指标体系汇合。
