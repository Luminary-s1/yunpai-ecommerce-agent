# M4 智能客服后端模块 — 交接说明

> **ARCHIVED（2026-08-07）**：旧分工与旧进度只作历史记录。当前 M4 五个工作包统一由
> 闫睿涵负责；实际状态只在负责人工作台网页维护，当前任务书见
> [M4_WORKBENCH.md](../M4_WORKBENCH.md)。

面向胡磊，承接**工作包「客服对话引擎端到端联调与交付」**。读完这一份加根目录的
`CONTRIBUTING.md` 就能开工。

---

## 1. 分工

| 承接人 | 工作包 | 类型 | 进度 |
|---|---|---|---:|
| 闫睿涵（模块负责人） | 客服会话管理与上下文控制 | 开发 | 100% |
| 闫睿涵（模块负责人） | 知识库增强回复生成 Pipeline | 开发 | 100% |
| 闫睿涵（模块负责人） | 客服意图识别与路由逻辑 | 开发 | 100% |
| 闫睿涵（模块负责人） | 客服模块效果评测与调优 | 开发 | 100% |
| **胡磊** | 客服对话引擎端到端联调与交付 | 验收出口 | 0% |

工时以工作台为准，本文档不重复。

### 一条不能变的约束

**本模块五个包里没有"测试"类型的包**，你这个包实际承担的就是 M4 的独立验收
出口——M4 的四个开发包全部由模块负责人承接，必须有人替他验收，这个人是你。

由此有一条硬性约束：**你不能再承接 M4 的任何开发包**。如果后续有人想把 M4 的
评测调优或其他开发活分给你，先提出来，不能默默接——接了就破坏验收独立性。

你在 M5 也是验收角色，在 M6 才是开发（取数适配 + 两个分析包）。

---

## 2. 你要交付什么

### 这一包不是从零开始

M4 的会话管理与回复生成两个包已经 100% 完成。**已经有了**：

- 多轮对话可用，能基于知识库回答商品咨询
- SSE 流式输出接口 `POST /v1/chat/stream`，事件协议
  （meta / delta / citations / handoff / done / error）
- 会话生命周期、Token 预算与滑动窗口截断
- 幂等设计（Idempotency-Key + `agent_invocations` 表，断连重放不重复回复）
- 虚拟店铺跨模块场景验收框架 **simulation-evidence-v1，当前 16 项**
- 本机顾客直测入口（`/admin` 智能客服页，无需客户端密钥）
- 客服管理后台：会话回放、人工任务、评测、场景验收、审计

### 四项交付，按顺序做

| 顺序 | 内容 | 状态 |
|---:|---|---|
| ~~1~~ | ~~流式输出端到端实跑证据~~ | ✅ 已完成，见 `docs/works/13-feature-m4-customer-service/EVIDENCE.md` |
| ~~2~~ | ~~新增场景 D17 多轮指代~~ | ✅ 已完成，模块负责人已合入 main（含反证） |
| 3 | 新增场景 D18 意图路由与低置信度转人工 | **可以直接开工**，无阻塞 |
| 4 | 交付文档 + 四份台账同步 | 等第 3 项 |

模块负责人的四个开发包（WP1–WP4）已全部完成并合入 `main`。意图路由与评测调优
的代码都在了，**D18 无阻塞，直接开工**。开工前拉一下 `upstream/main`，确认能
跑通全量测试。

### 关于 D18

D17 已由模块负责人完成（`simulation.py` + `test_virtual_store_simulation.py`
的 `test_d17_*` 系列），含反证。你只需要做 D18：

- 场景断言必须**确定性复现**，不能依赖模型每次输出的具体措辞
- D18：验证低置信度时确实走了转人工，而不是硬答
- 参照 D17 的写法（`simulation.py:1013` 的 `_verify_multi_turn_reference`）

### 验收角色的纪律

- **数值与行为类结论要人工核对**，不能只看接口返回码或测试是否变绿
- 发现的问题走缺陷清单回给模块负责人，**不要自己顺手改开发包的代码**——
  那会破坏开发与测试分离
- 你在 M6 是开发、在 M5 和 M4 是验收，别串了

---

## 3. 硬约束

### D-023 回答必须引用不可变上下文快照

不得绕过 `ContextBuilder` 取数。端到端验证时如果发现某条链路绕过了它，
这是缺陷，要报，不要在场景里将就过去。

### D-033 评测与模拟会话不污染真实数据

评测与模拟会话落 `evaluation` / `simulation` 来源，不写进 `operational`。
实跑证据同理，测试数据打显式虚拟标记。

### D-005 模型开关

`MODEL_ENABLED=false` 时不得发出任何模型请求。做流式实跑证据、跑评测时会开
模型，注意别把这条测挂了。

### D-010 业务意图不写入编排拓扑

不得新增按意图分支的节点或边。

### 其他

- 不新增第三方依赖，需要先提出来
- 场景证据契约（simulation-evidence-v1）是共享契约，改它之前先说一声
- 当前 `SCHEMA_VERSION = 25`，这两个包都不该动表；真要动先在群里占号

---

## 4. 代码提到哪里 / 环境 / 反证

这三节沿用旧 M6 交接规范，不重复写，直接看 `docs/tasks/archive/M6_HANDOFF.md` 的
第 4、5、6 节：

- **第 4 节**：fork 关系、分支命名、PR 提到哪里、schema 占号那个真实事故
- **第 5 节**：环境搭建、屏蔽代理的测试命令、用例数基线（main 是 352）
- **第 6 节**：反证要求与真实例子

分支命名这里用 `feature/m4-xxx`。

**针对本模块的反证**，至少这三处：

- 移除 D17 的指代消解逻辑后，多轮指代断言必须失败
- 移除低置信度转人工判定后，D18 的转人工断言必须失败

---

## 5. 相关文件索引

| 路径 | 内容 |
|---|---|
| `CONTRIBUTING.md` | 开发规范、PR 流程、项目决策、常见的坑 |
| `docs/tasks/M4_WORKBENCH.md` | 五个工作包的完整需求与验收标准 |
| `docs/tasks/archive/M6_HANDOFF.md` | 第 4/5/6 节：仓库、环境、反证（本文档不重复） |
| `docs/tasks/archive/PROGRESS_20260807.md` | 归档时的历史进度快照，不作为当前依据 |
| `docs/works/archive/12-feature-m5-operations-assistant/README.md` | 历史交付文档的格式参考 |
| `.project-to-act/PROJECT_OVERVIEW.md` | 完整的项目决策记录（D-001 起） |

---

## 6. 遇到这些情况先问再写

- 意图路由的置信度部分行为与预期不符
- 端到端验证发现的问题该算缺陷还是算需求没写清
- 要改 simulation-evidence-v1 场景证据契约
- 有人想把 M4 的开发包（比如评测调优）分给你——会破坏验收独立性
- 全量测试出现与你改动无关的既有失败
