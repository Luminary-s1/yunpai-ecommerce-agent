# 0.22.4 测试报告

日期：2026-07-23

## 变更范围

- 原 `/admin` 的“智能客服 -> 对话测试”改为本机直测；删除客户端 ID、主体标识和客户端密钥输入。
- 测试卡片直接调用回环受限的 `/v1/test/customer-chat`；预置晴川旗舰店商品上下文，提供保修、发货和转人工案例。
- 页面展示实际回答、风险/原因、人工接管、会话/追踪号和引用来源，并自动切换到 `simulation` 会话范围。
- 正式 `/v1/chat` 客户端认证未取消。

## 验证结果

| 检查 | 结果 |
|---|---|
| `py -3.12 -m pytest -q` | `223 passed in 415.57s` |
| `py -3.12 -m pytest tests/test_admin_console.py tests/test_api.py -q` | `7 passed in 24.51s` |
| 后台脚本 `node --check` | 通过 |
| 浏览器实测 `http://127.0.0.1:8104/admin` | 进入“智能客服”，点击“保修咨询”并发送；页面显示“晴川 AF5 空气炸锅整机保修 12 个月”，可见引用来源；客户端密钥输入控件数量为 0 |

## 边界

本机测试路径依旧需要显式设置 `CUSTOMER_TEST_ENABLED=true`，只接受回环请求，测试会话固定归入 `simulation/local-customer-test`。该验收不代表真实模型、真实渠道或生产放行。
