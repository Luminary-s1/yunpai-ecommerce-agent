# 云湃电商全域 AI 实现技术路径方案 V1

**版本：** 1.0  
**日期：** 2026-07-21  
**适用阶段：** 从当前客服 Agent 0.5.0 进入经营 AI 的产品开发和首批真实店铺 PoC  
**目标：** 给研发团队提供可以拆模块、建表、定义接口、排 Sprint 和验收的实施基线。

---

## 一、技术结论

### 1.1 总体路线

不推倒当前工程。保留现有 FastAPI、LangGraph、结构化决策、RAG、工具注册、权限门、幂等、后置验证、人工接管和审计，把它们从“客服问答链路”扩展为“经营任务安全内核”。

V1 采用 **本地部署的模块化单体 + 独立 Worker/Scheduler 进程 + PostgreSQL**：

- API、业务域和 Agent 仍在一个 Python 仓库，避免过早拆微服务；
- 同一代码包启动 `api`、`worker`、`scheduler` 三种进程；
- PostgreSQL 保存业务事实、任务、审批、执行和审计；
- 当前 SQLite 在开发和旧客服兼容期保留，生产经营数据迁往 PostgreSQL；
- 长任务采用数据库持久队列和 outbox，不在 V1 引入 Kafka、Temporal 或 Kubernetes；
- 原始文件和素材使用本地对象目录，元数据入库；后续需要多节点时再切 MinIO/S3；
- 平台和 ERP 是事实源及执行器，云湃是经营控制面，不重做 ERP/WMS。

### 1.2 V1 交付范围

V1 必须交付：

1. 运营人员、角色、组织、店铺范围权限；
2. 店铺、商品、渠道 Listing、SKU 映射、订单、退款、库存、广告日指标和费用的统一数据模型；
3. CSV/Excel 标准导入连接器和 1 个真实 ERP 或平台连接器；
4. 可追溯的经营指标语义层；
5. 每日经营简报、异常检测、SKU 和库存诊断；
6. 内容草稿生成与事实/品牌规则检查；
7. 建议转任务、审批、执行记录和人工交接；
8. 只读工具和至少 1 个低风险写工具；
9. 运营工作台、审批中心和审计查询；
10. 真实数据影子运行、评测集和上线门禁。

V1 不做：

- 自建 WMS、财务总账、广告实时竞价引擎；
- 未经平台允许的浏览器注入、Cookie 抓取或模拟客服客户端；
- 自动退款、赔付、改价、采购、付款和大额广告调节；
- 让多个自由 Agent 互相讨论后直接写业务系统；
- 自动修改权限、风险规则、指标口径、Prompt 或代码。

---

## 二、当前代码基线与缺口

### 2.1 可以直接复用

| 当前模块 | 已有能力 | V1 用法 |
|---|---|---|
| `auth.py` | 客户端/管理员认证、租户绑定、主体哈希 | 扩展为运营用户、角色和店铺数据范围 |
| `graph.py` | 有界 ReAct、结构化路由、工具观察与执行 | 保留为交互式 Agent；新增事件任务入口和领域工作流 |
| `tools.py` | Pydantic 参数、读写分类、幂等字段、策略和验证器 | 升级为带风险、权限、审批和能力声明的统一工具 SDK |
| `rag.py` / `evolution.py` | 租户知识、来源、审批、回归和回滚 | 扩展品牌规范、商品事实、平台规则、经营 SOP |
| `handoff.py` | 持久任务、状态机、乐观锁 | 抽象成通用经营任务，客服交接成为一种任务类型 |
| `database.py` | SQLite WAL、迁移、审计、会话和指标 | 保留兼容；新增 PostgreSQL repository 和 Alembic |
| `api.py` | FastAPI、限流、请求限制、健康检查 | 增加运营 API、连接器 API、任务/审批和数据查询 |
| `policy.py` | 输入/输出安全门 | 增加动作风险、预算、数据范围、营销和内容合规策略 |

### 2.2 必须补齐

- 当前身份只有 `client/admin`，无法表达运营、主管、财务、仓库和审计岗位；
- 当前业务由聊天触发，缺少定时同步、平台消息和经营异常事件；
- 当前库没有商品、订单、库存、广告、费用、指标和数据血缘；
- 工具没有连接器能力版本、审批策略、dry-run、外部请求 ID 和补偿信息；
- 人工任务绑定客服 session，不能承载独立经营任务；
- SQLite 不适合高频同步、复杂聚合和多进程并发写；
- 没有运营前端、数据质量监控和连接器运行看板；
- 当前测试覆盖 Agent 安全链路，但没有业务数据对账、连接器契约和经营指标金标准。

---

## 三、目标架构

```text
                         ┌─────────────────────────────┐
                         │  Web 经营工作台（React）     │
                         └──────────────┬──────────────┘
                                        │ HTTPS / SSE
┌───────────────────────────────────────▼─────────────────────────────────────┐
│ FastAPI 模块化单体                                                        │
│ Identity │ Catalog │ Orders │ Inventory │ Marketing │ Finance │ Knowledge │
│ Metrics  │ Tasks   │ Approvals │ Actions │ Audit │ Agent API │ Connectors │
└──────────────┬──────────────────────┬──────────────────────┬────────────────┘
               │                      │                      │
       ┌───────▼────────┐     ┌───────▼────────┐     ┌──────▼─────────┐
       │ Agent Runtime  │     │ Policy Engine  │     │ Connector SDK  │
       │ LangGraph/RAG  │     │ 权限/预算/风险  │     │ ERP/平台/文件   │
       └───────┬────────┘     └───────┬────────┘     └──────┬─────────┘
               │                      │                      │
┌──────────────▼──────────────────────▼──────────────────────▼────────────────┐
│ PostgreSQL：事实、指标、事件、任务、审批、动作、执行、审计、同步游标       │
└──────────────┬─────────────────────────────────────────────┬────────────────┘
               │                                             │
       ┌───────▼────────┐                            ┌───────▼────────┐
       │ Worker         │                            │ Scheduler      │
       │ 同步/归一/执行  │                            │ 定时拉取/日报   │
       └───────┬────────┘                            └────────────────┘
               │
       ┌───────▼──────────────────────────────────────────────────────┐
       │ 官方平台 API / ERP / CRM / 客服 / 广告 / 财务 / 文件导入    │
       └──────────────────────────────────────────────────────────────┘
```

### 3.1 模块边界

| 模块 | 责任 | 不负责 |
|---|---|---|
| Identity | 用户、角色、组织、店铺范围和服务账号 | 平台 OAuth 凭据内容 |
| Connector | 授权、拉取、回调、限流、重试、游标、原始报文 | 业务指标和 AI 决策 |
| Commerce Facts | 标准商品、订单、退款、库存、广告、费用 | 修改来源系统原始事实 |
| Metrics | 指标定义、查询 DSL、快照、异常检测 | 让模型自由拼 SQL |
| Knowledge | 商品事实、品牌规范、平台规则、SOP 和版本 | 权限、金额阈值和审批规则 |
| Agent | 理解目标、取证、生成建议和选择已注册工具 | 直接放行高风险动作 |
| Policy | 权限、风险、预算、频控、审批和内容规则 | 自然语言规划 |
| Task/Approval | 任务、负责人、状态、审批链、期限和验收 | 代替外部系统执行动作 |
| Action/Execution | 幂等、dry-run、调用、读回、补偿和结果 | 依据模型语气判断成功 |
| Audit | 不可抵赖的关键操作轨迹 | 保存模型隐式思维过程 |

---

## 四、代码仓库改造

建议在现有仓库内逐步形成以下结构：

```text
src/ecommerce_agent/
  api/
    public.py             # 健康、登录、会话
    operations.py         # 驾驶舱和经营查询
    tasks.py              # 任务和审批
    connectors.py         # 授权、同步和连接器状态
    admin.py              # 规则、知识、用户和审计
  application/
    command_bus.py
    query_bus.py
    services/
  domain/
    identity/
    catalog/
    orders/
    inventory/
    marketing/
    finance/
    metrics/
    tasks/
    actions/
  agents/
    supervisor.py
    operating_analyst.py
    content_agent.py
    customer_service.py   # 迁移当前客服图
    state.py
    prompts/
  connectors/
    base.py
    registry.py
    file_import/
    erp_<selected>/
    platform_<selected>/
  tools/
    registry.py
    commerce_read.py
    content.py
    task_tools.py
    low_risk_write.py
  policy/
    action_policy.py
    data_scope.py
    content_policy.py
  infrastructure/
    db/
      models.py
      repositories.py
      migrations/
    jobs/
    secrets/
    object_store/
    observability/
  legacy/
    ...当前兼容代码...
web/
  src/
tests/
  unit/
  integration/
  contract/
  evals/
```

改造规则：

- 先抽接口再迁移实现，不进行一次性大重写；
- `AgentService` 逐步只做应用编排，不直接持有所有基础设施；
- 领域模块不得直接调用第三方 HTTP，必须通过 Connector；
- Agent 工具不得直接写表，必须调用应用层 Command；
- 所有时间内部使用 UTC，界面按租户时区展示；
- 金额用 `Decimal + currency`，禁止 `float`；
- 外部 ID 必须与 `source_system + store_id` 组成唯一键。

---

## 五、核心数据模型

### 5.1 身份和连接

| 表 | 关键字段 |
|---|---|
| `tenants` | `id, name, timezone, status` |
| `users` | `id, tenant_id, login, display_name, status` |
| `roles` / `user_roles` | 角色和用户关系 |
| `permissions` / `role_permissions` | `resource, action, risk_ceiling` |
| `user_store_scopes` | 用户可访问的品牌、渠道和店铺范围 |
| `channels` | 淘宝、京东、抖店、ERP 等来源定义 |
| `stores` | `channel_id, external_id, name, currency, status` |
| `connector_instances` | 类型、能力版本、状态、授权到期时间 |
| `credential_refs` | 只保存 SecretStore 引用，不保存明文凭据 |
| `sync_cursors` | 资源类型、游标、窗口、最后成功时间和水位 |

### 5.2 商品与交易事实

| 表 | 关键字段与约束 |
|---|---|
| `products` | 品牌级 SPU；事实状态和版本 |
| `skus` | 品牌 SKU、条码、成本、重量和规格 |
| `listings` | 平台商品；`store_id + external_listing_id` 唯一 |
| `listing_skus` | 渠道 SKU 与品牌 SKU 映射、置信度、确认人 |
| `orders` | 来源订单、状态、金额、币种、时间；买家使用租户内哈希 ID |
| `order_lines` | Listing/SKU、数量、成交价、优惠分摊和成本快照 |
| `refunds` / `after_sales` | 类型、状态、金额、责任、证据和平台结果 |
| `inventory_balances` | SKU、仓、库存类型、数量、来源时间和版本 |
| `inventory_movements` | 入库、出库、占用、释放、调拨的不可变流水 |
| `ad_metrics_daily` | 店铺、账户、计划、商品、消耗、曝光、点击、成交 |
| `fee_entries` | 平台费、佣金、运费、广告、退款、其他费用及分摊 |

### 5.3 控制面数据

| 表 | 用途 |
|---|---|
| `raw_events` | 原始报文哈希、来源、游标、schema 版本、对象存储引用 |
| `domain_events` | 标准事件信封；供异常、指标和任务消费 |
| `metric_definitions` | 指标 ID、名称、版本、单位、维度、SQL 模板和负责人 |
| `metric_snapshots` | 已计算指标值、数据窗口、定义版本和质量状态 |
| `anomalies` | 异常类型、基线、观测值、严重度、证据和状态 |
| `recommendations` | Agent 建议、证据快照、预期影响、风险和有效期 |
| `tasks` | 类型、Owner、状态、期限、验收条件和来源建议 |
| `approval_requests` | 对象、动作、风险、金额、审批策略和状态 |
| `actions` | 标准动作、dry-run 结果、幂等键和预期后置条件 |
| `action_executions` | 每次调用、外部请求 ID、响应、读回和补偿状态 |
| `audit_events` | Actor、资源、动作、前后版本、trace 和不可变摘要 |

### 5.4 数据约束

- `raw_events` 对 `tenant_id + connector_id + source_event_id + payload_hash` 建唯一索引；
- `orders`、`listings` 等来源对象使用版本或 `updated_at` 防止旧事件覆盖新状态；
- 指标快照必须绑定 `metric_definition_version`；
- 建议必须保存证据 ID，不复制不可验证的自然语言数字；
- 写动作必须保存 `idempotency_key`，租户内唯一；
- 审批通过只授权特定动作版本，动作内容变化后必须重新审批；
- 个人信息默认不进入 Agent checkpoint，地址和电话按字段级策略脱敏或不落库。

### 5.5 数据库迁移策略

1. 引入 SQLAlchemy 2.x repository 接口和 Alembic；
2. 先让新经营域只使用 PostgreSQL，旧客服仍读写 SQLite；
3. 将审计、运营用户和通用任务迁到 PostgreSQL；
4. 将客服消息和 checkpoint 是否迁移作为独立 ADR，不阻塞 V1；
5. 提供一次性导出脚本，把需要保留的知识和审计从 SQLite 导入 PostgreSQL；
6. 每次迁移支持向前升级、备份和演练，不承诺自动降级数据库 schema。

---

## 六、连接器技术规范

### 6.1 统一接口

```python
class Connector(Protocol):
    def capabilities(self) -> ConnectorCapabilities: ...
    def test_connection(self) -> ConnectionCheck: ...
    def pull(self, request: PullRequest) -> PullBatch: ...
    def verify_webhook(self, headers: dict[str, str], body: bytes) -> VerifiedEvent: ...
    def execute(self, action: ExternalAction) -> ExternalResult: ...
    def verify(self, action: ExternalAction, result: ExternalResult) -> VerificationResult: ...
```

`ConnectorCapabilities` 至少声明：

- 资源：商品、订单、售后、库存、广告、会员、客服、账单；
- 模式：`read / write / webhook / polling / bulk_export`；
- 版本、速率限制、最大时间窗口和分页方式；
- 是否支持 dry-run、幂等键、外部请求 ID 和读回；
- 所需平台权限包与授权到期时间；
- PII 字段和数据驻留说明。

### 6.2 标准事件信封

```json
{
  "event_id": "evt_...",
  "tenant_id": "tenant_...",
  "connector_id": "conn_...",
  "store_id": "store_...",
  "resource": "order",
  "event_type": "order.updated",
  "source_id": "platform-order-id",
  "source_version": "2026-07-21T02:30:00Z",
  "occurred_at": "2026-07-21T02:29:10Z",
  "received_at": "2026-07-21T02:30:03Z",
  "schema_version": 1,
  "payload_ref": "objects/sha256/...",
  "payload_hash": "sha256:..."
}
```

### 6.3 同步链路

```text
定时拉取/回调
  -> 验签与授权范围检查
  -> 原始报文落地、哈希去重
  -> 标准化与 schema 校验
  -> 业务 upsert（防旧版本覆盖）
  -> 写 domain_event + outbox
  -> 更新游标和水位
  -> 指标增量计算/异常检测
  -> 必要时创建建议或任务
```

### 6.4 错误分类

| 错误 | 是否重试 | 处理 |
|---|---|---|
| 401/授权过期 | 否 | 连接器置为 `reauth_required`，告警并停止写操作 |
| 429/平台限流 | 是 | 按 `Retry-After` 和指数退避，保持游标 |
| 5xx/网络超时 | 是 | 有上限重试，超过后进入死信和人工处理 |
| schema 变化 | 否 | 原文保留，标准化失败告警，禁止静默丢字段 |
| 重复/乱序 | 不重试 | 按唯一键和来源版本消解 |
| 写入结果不确定 | 谨慎 | 禁止盲目重试；先按幂等键或业务对象读回 |
| 平台业务拒绝 | 否 | 保存错误码、规则版本并转任务 |

### 6.5 首个连接器选择

开发顺序固定为：

1. `file_import`：用脱敏 CSV/Excel 建立稳定契约和回放数据；
2. 客户现有 ERP：在聚水潭、旺店通或其他实际系统中选择 API 权限最完整的一家；
3. 首个平台只读连接器：选择客户订单量最大的淘宝或抖店；
4. 平台写能力在只读对账连续两周通过后再开发。

第 2 步必须在 Sprint 0 完成权限预检，否则不允许把该连接器写入发布日期。

---

## 七、指标语义层与经营诊断

### 7.1 指标必须代码化

首批指标：

- 支付 GMV、净销售额、退款金额与退款率；
- 商品毛利、店铺贡献毛利、广告后利润；
- 广告消耗、成交、ROAS、ROI 和新客成本；
- 可售库存、库存周转天数、预计缺货日、滞销库存金额；
- 客服首次响应、解决率、转人工率、退款/投诉关联；
- 复购率、会员销售占比和流失风险。

每个指标定义包含：业务名称、公式、事实表、过滤条件、时间字段、币种、税口径、退款口径、维度、负责人、版本和生效时间。

### 7.2 禁止模型直接查询原始表

自然语言查询流程：

```text
用户问题
  -> 模型只选择 metric_id、时间范围、维度、筛选和排序
  -> Pydantic 校验 QuerySpec
  -> 数据范围策略校验
  -> 语义层编译参数化 SQL
  -> 返回结构化结果、定义版本和数据水位
  -> 模型解释结果，不改写数值
```

### 7.3 异常检测 V1

先用可解释方法，不先上复杂预测模型：

- 同比/环比和移动中位数偏离；
- 固定业务阈值；
- 库存覆盖天数和交期规则；
- 广告消耗增加但成交/毛利下降；
- 退款、差评、客服问题与 SKU 的相关抬升；
- 数据水位落后、对账差异和缺失字段。

异常记录必须区分“业务异常”和“数据异常”。数据质量不通过时不得生成经营结论。

---

## 八、Agent 与确定性工作流

### 8.1 两类运行时分离

**Agent Runtime：** 用于理解模糊目标、选择查询、组合证据、生成建议和草稿。继续使用 LangGraph，有界步骤和结构化输出。

**Workflow Runtime：** 用于同步、审批、调用、重试、读回和补偿。使用确定性状态机和数据库任务，不交给 LLM 决定状态迁移。

### 8.2 V1 Agent 拓扑

```text
request/event
  -> identity_and_scope
  -> data_quality_gate
  -> supervisor（选择 analysis/content/service/task）
  -> domain_retrieval + metric tools
  -> typed recommendation or draft
  -> policy_gate
  -> task/approval/action proposal
  -> persist evidence
```

V1 只保留一个 Supervisor 和三个领域能力：

- `operating_analyst`：日报、异常、SKU/库存分析；
- `content_agent`：商品文案和渠道适配草稿；
- `customer_service`：迁移现有客服能力。

不建立十几个可自由通信的 Agent。后续只有当领域 Prompt、工具和评测明显分化时才拆分。

### 8.3 建议结构

```json
{
  "recommendation_type": "inventory_risk",
  "title": "SKU-A 预计 6 天后缺货",
  "summary": "...",
  "evidence_ids": ["metric_snapshot_1", "inventory_balance_2"],
  "assumptions": ["近14日销量代表未来7日需求"],
  "confidence": "medium",
  "expected_impact": {"stockout_days_avoided": 4},
  "risk_level": "L1",
  "proposed_actions": ["create_replenishment_task"],
  "expires_at": "2026-07-23T00:00:00Z"
}
```

`confidence` 不能替代风险门。高置信度的 L4 动作仍必须审批。

---

## 九、工具和动作设计

### 9.1 扩展 ToolSpec

在当前 `ToolSpec` 增加：

- `domain`、`capability_version`；
- `risk_level: L0-L5`；
- `required_permissions`；
- `approval_policy_id`；
- `supports_dry_run`；
- `data_classification`；
- `rate_limit_key`；
- `compensation_handler` 或明确 `non_compensatable`；
- `result_schema` 和 `verification_timeout`。

工具目录向模型暴露说明和输入 schema，但不暴露凭据、内部阈值或其他租户数据。

### 9.2 V1 工具清单

**L0 只读：**

- `get_kpi_summary`
- `list_business_anomalies`
- `get_sku_performance`
- `get_inventory_risk`
- `get_order_after_sale_summary`
- `get_ad_profitability`
- `search_approved_knowledge`

**L1 草稿与内部动作：**

- `create_content_draft`
- `validate_content_claims`
- `create_internal_task`
- `attach_evidence_to_task`
- `request_approval`

**首个 L2/L3 写工具：**

建议选择 `publish_listing_draft` 或 `update_safety_stock_buffer` 其中一个，条件是目标系统提供官方 API、dry-run 或稳定读回。不要把退款或广告预算作为第一个写工具。

### 9.3 写动作状态机

```text
draft
  -> policy_checked
  -> approval_required / approved
  -> queued
  -> executing
  -> verifying
  -> succeeded
  -> failed / uncertain / compensation_required
  -> compensated / manually_resolved
```

只有 `succeeded` 且后置条件满足，前端和 Agent 才能使用“已完成”。

---

## 十、任务和审批

### 10.1 通用任务状态

`proposed -> accepted -> working -> review -> completed`，并支持 `rejected / input_required / failed / canceled`。沿用当前 handoff 的乐观锁版本。

### 10.2 审批策略示例

| 动作 | 条件 | 审批 |
|---|---|---|
| 创建内部异常任务 | L1 | 无需审批 |
| 发布内容草稿 | 普通类目、无敏感声明 | 运营一审 |
| 修改安全库存 | 变化不超过配置阈值 | 仓储主管 |
| 广告预算调整 | 任意金额，V1 | 运营主管；V1 不自动执行 |
| 改价、退款、采购、付款 | L4 | V1 只生成方案，禁止执行 |

审批依据固定保存：动作 diff、证据、风险、影响对象、金额/数量、回滚方式和有效期。

---

## 十一、API 设计

### 11.1 经营查询

```text
GET  /v1/ops/overview?from=&to=&store_ids=
GET  /v1/ops/anomalies
GET  /v1/ops/products/{sku_id}/performance
POST /v1/ops/query                 # 受控 QuerySpec，不接收原始 SQL
POST /v1/ops/briefings/generate
```

### 11.2 任务、建议和动作

```text
GET  /v1/recommendations
POST /v1/recommendations/{id}/accept
POST /v1/recommendations/{id}/reject
GET  /v1/tasks
POST /v1/tasks/{id}/transition
POST /v1/actions/dry-run
POST /v1/actions/{id}/request-approval
POST /v1/approvals/{id}/decide
GET  /v1/actions/{id}/executions
```

### 11.3 连接器

```text
GET  /v1/connectors/catalog
POST /v1/connectors/instances
POST /v1/connectors/{id}/test
POST /v1/connectors/{id}/sync
GET  /v1/connectors/{id}/runs
POST /v1/webhooks/{connector_type}/{instance_id}
```

### 11.4 并发和版本

- 所有可变资源返回 `version` 或 ETag；
- 更新要求 `If-Match` 或 `expected_version`；
- 批量动作先创建 `action_batch`，每个对象独立执行和验收；
- API 返回 `trace_id`、`data_as_of` 和 `schema_version`；
- 大导出和 Agent 长任务返回 `202 + job_id`，禁止长时间占用 HTTP 请求。

---

## 十二、经营工作台

建议技术栈：React + TypeScript + Vite，TanStack Query/Table，ECharts；由 FastAPI 提供 API，生产构建可由同一一体机反向代理。V1 页面：

1. `/overview`：关键指标、数据水位、异常和今日任务；
2. `/products`：SKU 映射、销售、毛利、库存和内容状态；
3. `/inventory`：缺货、滞销、在途和建议；
4. `/briefings`：日报、证据和负责人确认；
5. `/content`：草稿、事实检查、版本和审批；
6. `/tasks`：任务队列、状态、期限和验收；
7. `/approvals`：动作 diff、证据、风险和批准/拒绝；
8. `/audit`：按用户、店铺、动作和 trace 查询；
9. `/settings/connectors`：授权、同步水位、失败和权限能力。

操作型页面保持信息密度，禁止把关键指标、审批和错误藏在聊天窗口。聊天/Agent 输入是辅助入口，不是唯一入口。

---

## 十三、安全与隐私

### 13.1 身份权限

- 使用短期访问令牌和可撤销刷新会话；服务连接器使用独立服务身份；
- RBAC 负责岗位能力，数据范围单独约束品牌/渠道/店铺；
- 高风险动作要求近期二次认证；
- 管理员、审计员和业务审批人职责分离；
- 默认拒绝，权限和动作风险上限均由代码策略决定。

### 13.2 密钥

- 定义 `SecretStore` 抽象；数据库只保存引用；
- 一体机主密钥由操作系统凭据、TPM 或受保护密钥文件封装；
- 平台 token 使用信封加密并记录轮换/到期，不写日志；
- 环境变量只用于首次引导，不作为长期多连接器密钥仓库。

### 13.3 模型数据边界

- 工具先聚合/脱敏，再向模型提供最小上下文；
- 买家姓名、电话、详细地址、身份证、银行卡和平台 token 不发送给模型；
- Prompt 和模型输出落审计时再次脱敏；
- 严格本地客户可切本地模型，但必须使用同一评测门槛；
- 模型供应商切换不得改变权限和工具放行逻辑。

---

## 十四、部署与运维

### 14.1 进程

- `web`：Nginx/Caddy，HTTPS 和静态前端；
- `api`：FastAPI 2-4 workers；
- `worker`：同步、标准化、指标、Agent 长任务和动作执行；
- `scheduler`：定时拉取、日报和留存；
- `postgres`：业务主库；
- 可选 `model-runtime`：严格本地模型。

V1 使用 Docker Compose 或受控系统服务；不使用 Kubernetes。

### 14.2 硬件档位

| 档位 | 建议配置 | 用途 |
|---|---|---|
| PoC 混合模型 | 8-16 核 CPU、32GB RAM、2TB NVMe | 本地数据 + 外部模型 API，3-5 店 |
| 标准生产 | 16-24 核、64GB RAM、2-4TB 企业级 NVMe、UPS | 50 店以内、数据和任务本地化 |
| 严格本地模型 | 标准生产配置 + 满足模型的 GPU/显存 | 数据不出域；模型效果需单独验收 |

容量验收目标：50 店、10 万渠道 SKU、每年 500 万订单、20 名并发运营用户；常规增量数据 15 分钟内可见，日报在计划时间后 10 分钟内生成。

### 14.3 备份恢复

- PostgreSQL 每日全备 + WAL 增量，备份加密；
- 原始文件和素材按内容哈希增量备份；
- 每季度执行一次恢复演练；
- PoC 建议 RPO 24 小时、RTO 4 小时；生产目标 RPO 15 分钟、RTO 2 小时；
- 授权凭据恢复后必须验证可用性，不把“数据库恢复成功”等同于系统恢复。

### 14.4 观测指标

- 连接器：延迟、水位、错误率、限流、授权到期、漏单对账；
- 数据：缺失、重复、乱序、SKU 未映射和指标差异；
- Agent：工具选择正确率、事实错误、建议采用率、人工修改率；
- 动作：dry-run、审批、执行、读回、失败、不确定和补偿；
- 系统：请求延迟、队列深度、DB、磁盘、备份和模型用量。

---

## 十五、测试与上线门禁

### 15.1 测试层次

| 层次 | 内容 |
|---|---|
| 单元测试 | 指标公式、状态机、权限、风险、映射和标准化 |
| 连接器契约测试 | 固定报文、分页、游标、限流、乱序、授权过期和 schema 变化 |
| 集成测试 | PostgreSQL、Worker、outbox、API、审批和动作读回 |
| 回放测试 | 60-90 天脱敏真实数据，验证漏单、重复和对账 |
| Agent eval | 不少于 300 条金标准任务，包含事实、工具、拒绝和转人工 |
| 故障注入 | 429、5xx、超时、重复回调、磁盘满、模型不可用和 DB 重启 |
| 安全测试 | 越权、提示注入、跨租户、凭据泄露、批量导出和审计完整性 |

### 15.2 硬门槛

- 严重事实错误 0；
- 跨租户/跨店越权 0；
- 未授权写操作 0；
- 高销量 SKU 映射 100%，动销 SKU 映射至少 95%；
- 订单/退款/广告/费用与来源系统按约定口径对账通过；
- 写动作只有后置验证通过才标记成功；
- 所有失败和数据延迟对运营可见；
- 备份恢复、授权失效和模型故障降级演练通过。

---

## 十六、16 周开发计划

按 6-8 人团队估算：后端 3、前端 1-2、数据/算法 1、测试 1、产品/实施 1；安全和运维可兼职但必须明确 Owner。

| Sprint | 周期 | 交付 | 退出条件 |
|---|---:|---|---|
| 0 | 第 1-2 周 | ADR、范围、真实客户权限预检、数据字典、PostgreSQL/Alembic 基线 | 首个 ERP/平台权限有书面清单；样本数据到位 |
| 1 | 第 3-4 周 | 运营身份、RBAC、店铺范围、连接器 SDK、数据库任务队列 | 跨店越权测试通过；任务可恢复 |
| 2 | 第 5-6 周 | 文件导入、原始事件、商品/SKU/订单/退款/库存归一 | 30 天数据可重复导入且无重复；对账初步通过 |
| 3 | 第 7-8 周 | 指标语义层、数据水位、驾驶舱和异常 V1 | 核心指标可复算；数据异常阻断经营结论 |
| 4 | 第 9-10 周 | 经营分析 Agent、证据引用、日报和 150 条 eval | 严重事实错误 0；人工盲评达标 |
| 5 | 第 11-12 周 | 内容 Agent、通用任务、审批和审计工作台 | 草稿事实检查、版本和审批闭环通过 |
| 6 | 第 13-14 周 | 首个真实连接器只读同步、断点续传和对账 | 连续两周影子同步或等价回放通过 |
| 7 | 第 15-16 周 | 首个低风险写工具、dry-run、读回、补偿、300 条 eval | 未授权写入 0；后置验证成功率达到门槛 |

如首个真实连接器权限在第 2 周仍未确认，Sprint 6-7 自动降级为文件导入 PoC，不伪装为平台实时接入。

---

## 十七、首批开发 Backlog

按依赖顺序建立以下 Epic：

1. `EPIC-IDENTITY`：运营用户、RBAC、店铺范围和二次认证；
2. `EPIC-POSTGRES`：Repository、Alembic、审计和任务迁移；
3. `EPIC-CONNECTOR-SDK`：能力、SecretStore、游标、原始事件和错误规范；
4. `EPIC-FILE-IMPORT`：标准模板、校验、预览、导入和回放；
5. `EPIC-COMMERCE-FACTS`：商品/SKU/订单/退款/库存；
6. `EPIC-METRICS`：指标注册、QuerySpec、快照、水位和异常；
7. `EPIC-OPS-UI`：驾驶舱、商品、库存、任务、审批和连接器状态；
8. `EPIC-ANALYST-AGENT`：受控查询、证据、建议和日报；
9. `EPIC-CONTENT-AGENT`：品牌规则、商品事实、草稿和审核；
10. `EPIC-TASK-APPROVAL`：通用状态机、Owner、期限、验收和乐观锁；
11. `EPIC-REAL-CONNECTOR-1`：权限确认后的 ERP/平台连接器；
12. `EPIC-ACTION-RUNTIME`：dry-run、幂等、执行、读回和补偿；
13. `EPIC-EVALS`：300 条金标准、回放、故障注入和发布门禁；
14. `EPIC-APPLIANCE-OPS`：部署、备份、恢复、升级、监控和日志脱敏。

每个 Epic 的 Definition of Done：代码、迁移、接口文档、单元/集成测试、审计事件、运维指标、权限测试和用户操作说明同时完成。

---

## 十八、开发启动前必须确认的五个决策

1. **首个行业和客户：** 没有真实业务样本，不开始设计“通用全行业指标”。
2. **首个 ERP/平台连接器：** 以已获得权限为准，不以厂商官网宣传为准。
3. **V1 核心利润口径：** GMV、净销售、退款、平台费、广告费、运费和商品成本如何归集。
4. **部署模式：** 默认混合模型还是严格本地模型；这会直接影响硬件和评测。
5. **第一个写工具：** 必须是可逆、可读回、低资损且官方允许的动作。

上述五项由产品、技术、业务和首批客户共同签字确认，形成 ADR 和项目 Gate。未确认项不能由开发人员自行假设。

---

## 十九、V1 成功定义

V1 成功不是“Agent 能回答电商问题”，而是：

- 真实店铺数据可稳定进入统一事实层；
- 指标可复算、口径可追踪、数据延迟可见；
- AI 能从事实中发现高价值异常并生成有证据的建议；
- 建议能进入负责人、审批、执行和验收闭环；
- 第一个真实写动作在权限、幂等、读回和故障恢复下可靠运行；
- 任何模型不可用时，数据、任务、审批和人工流程仍然可工作。

达到这些条件后，再扩展广告、会员、供应链和财务 Agent，技术风险和商业风险都更可控。

